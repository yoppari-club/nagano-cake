<div class="container">
  <div class="row">
      <div class="col-md-4">
        <%= image_tag 'no_image.jpg' %>
      </div>
      <div class="col-md-8">
        <h3><%= @item.name %></h3>
        <p><%= @item.description %></p>
        <p>¥ <%= change_price_excluding_tax( @item.price_excluding_tax) %> (税込)</p>
        <p><%= render "item_amount_form", cart_item: @cart_item, item: @item %></p>
      </div>
  </div>
</div>

